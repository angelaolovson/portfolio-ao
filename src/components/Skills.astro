---
import type { Skill } from '~/types/skill';

type Props = {
    skills: Skill[];
}
const props = Astro.props as Props;
const { skills } = props;
---

<div>
    {skills.map((skill) => (
        <div>{skill.skillTitle}</div>
        <div>{skill.skillInfo}</div>
    ))}
</div>
